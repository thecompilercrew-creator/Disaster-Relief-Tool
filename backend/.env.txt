# MongoDB Connection String
# For local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/disaster_relief

# For MongoDB Atlas (Production - Replace with your actual connection string):
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/disaster_relief?retryWrites=true&w=majority

# JWT Secret Key (IMPORTANT: Generate a strong random key for production)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-random-string

# Server Port
PORT=5000

# Node Environment (development/production)
NODE_ENV=development